# View 分支 PR：View 系统对齐 + 跨模块规范落地（block.number 时间口径）+ 文档/测试矩阵/脚本

## 📋 概述

本 PR 为 **view 分支**的合入请求，主要完成：

1. **View 工作指南文档迁移与路径统一**：将 `ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` 从 `src/Vault/view/` 迁至 `docs/Usage-Guide/`，并更新全仓库内所有引用路径。
2. **View 系统与架构指南对齐**：在文档与实现层面落实 `docs/Architecture-Guide.md` 对 View 的职责、权限（Scheme U）、缓存（B 类 meta）、DataPush、批量错误口径等 MUST 约束，并补充/收口测试矩阵与自检脚本。
3. **时间依赖改造（全局规范）**：时间依赖改造为**全仓库**规范，由 `docs/Usage-Guide/Time-Dependency-Refactor-Guide.md` 定义（链上门槛仅用 `block.number`，不用秒级时间）；覆盖抵押物/出借/撮合/还款/清算/费用/治理/降级/预言机/奖励/白名单/View 等各段。本 PR 落实其中一部分，**包含 View 层**（缓存 TTL/有效性/同步间隔与命名统一为 block 口径）及文档中 DONE 清单所涉的其它模块与测试/脚本。
4. **测试与验收工具链**：新增 View 矩阵自检脚本（`view-matrix-selfcheck.ts`）、若干 e2e/acceptance 与 smoke 脚本，以及配套文档（scripts/tests、scripts/e2e 的 README 等），便于审查与 CI 验收。
5. **跨域高影响改动（非纯 View）**：为确保 View 与 `docs/Architecture-Guide.md` 的 MUST 约束可落地（权限/缓存 meta/DataPush/批量错误口径/时间口径等），同步调整了大量 **核心合约、接口与基础库**（如 `src/core/`、`src/libraries/`、`src/interfaces/`、`src/Vault/modules/`、`src/Vault/liquidation/`、`src/Reward/`、`src/monitor/`、`src/registry/`），以及配套 tests/scripts/configs/deployments/frontend-config。

**目标读者**：审查 view 分支的同事；希望快速了解「改了哪些、为什么改、如何验证」的 reviewer。

---

## 📊 变更统计（view 分支相对 base）

- **总体（以当前工作区计划合入内容为准）**：`323 files changed, 20287 insertions(+), 14254 deletions(-)`（来自 `git diff --shortstat`）
- **按状态拆分（来自 `git status --porcelain`）**
  - **修改（M）**：316（其中 **1 个为 staged**，其余为 unstaged）
  - **新增（??）**：25
  - **删除（D）**：7

> 注：上述统计反映的是 **当前工作区** 相对 base 的差异；如在合并前继续提交、rebase 或拆分 PR，数字会随之变化。

建议审查时**按模块分批**：先文档与路径、再 View 合约与测试、最后脚本与配置。

---

## 🎯 主要变更领域

### 1. 文档迁移与引用更新

| 变更 | 说明 |
|------|------|
| **ARCH-VIEW-ALIGNMENT-WORKGUIDE.md** | 从 `src/Vault/view/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` **迁移**到 `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` |
| **引用路径统一** | 以下文件中原先指向 `src/Vault/view/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` 的引用均已改为 `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`：<br>• `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`（自身位置与 rg 示例）<br>• `scripts/tests/view-matrix-selfcheck.ts`（workguidePath）<br>• `src/Vault/view/README.md`（依据文档）<br>• `docs/Usage-Guide/StatisticsView-Strict-B-Push-Pipeline-Implementation-Checklist.md`（目标/附录） |

其他仅提及「ARCH-VIEW-ALIGNMENT-WORKGUIDE.md」文件名（未写路径）的文档与脚本保持不变，语义仍指向同一份工作指南。

### 2. View 工作指南与测试矩阵（docs/Usage-Guide）

- **ARCH-VIEW-ALIGNMENT-WORKGUIDE.md**  
  - 位置：`docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`  
  - 内容：View 模块清单、Registry Key、权限（Scheme U）、DataPush、B 类缓存、批量错误口径、测试矩阵（§4.x）与验收标准（§5.1）。  
  - 状态：临时工作文档；View 全部改造验收通过后可删除（文档内已说明）。
- **StatisticsView-Strict-B-Push-Pipeline-Implementation-Checklist.md**  
  - 与 Architecture-Guide + ARCH-VIEW-ALIGNMENT-WORKGUIDE 对齐的 Statistics 严格 B+ 实施清单。
- 其他 Usage-Guide 下新增/更新文档（如 No-Empty-Blocks-Findings、Time-Dependency-Refactor-Guide、User-Dimensional-View-Read-Policy-Guide 等）按需配合 View 与规范落地。

### 3. 时间依赖改造（全局规范，本 PR 涉及范围）

**时间依赖改造为全仓库规范**，不是仅 View 层：`docs/Usage-Guide/Time-Dependency-Refactor-Guide.md` 规定**链上门槛与缓存有效性仅使用 `block.number`**（不使用秒级时间），适用范围包括：

- **资金链各段**：抵押物（VaultCore/VaultRouter/CollateralManager）、出借/意向（VaultBusinessLogic、SettlementIntentLib）、撮合/放款（LendingEngine、LoanNFT）、还款/结算（SettlementManager、VaultLendingEngine）、提前还款保证金（GuaranteeFundManager、EarlyRepaymentGuaranteeManager）、违约清算（ModuleCache、LiquidationConfig*、LiquidationRiskManager、LiquidationManager）、费用（FeeRouter/FeeRouterView）。
- **跨段共用**：Registry timelock、CrossChainGovernance、DegradationMonitor/Core/Storage、PriceOracle/CoinGeckoPriceUpdater、SystemUtils、configs、Reward/Token、AssetWhitelist/AuthorityWhitelist、VaultAdmin、以及 **View 层**（ViewCache、各 View 模块、接口与 Mock）。
- **盘点范围**：文档中 Step 1A 全仓 `timestamp` 命中约 185 个文件；改造模式与分段执行顺序见该指南「Step 2A / 2C / 分组表格」。

**本 PR 落实的内容**（与指南中 DONE 清单对应）：包含 **View 层**的 block 口径落地，以及本分支已改动的其它模块（如 LendingEngine、SettlementManager、清算/费用/治理/降级/预言机/奖励/白名单等，以实际 diff 为准）。

**View 层改造要点（本 PR 内可直接审查）：**

- **链上口径**：缓存 TTL / 快照有效性 / 同步间隔统一为**区块高度**（`updateBlock`、`maxAgeBlocks` 等）；不以秒级时间作门槛或有效性判断。
- **命名收敛**：`src/Vault/view/**` 下 `ts` → `blockNumber`，`CACHE_DURATION`/`SYNC_INTERVAL` 等 → `...Blocks` 或 ViewConstants；缓存有效性以 `block.number - updateBlock <= maxAgeBlocks` 判定，meta 统一 `isValid` + `blockNumber`（或 `updateBlock`）。
- **涉及文件**：ViewConstants、ViewCache、各 View 模块（AccessControlView、BatchView、CacheOptimizedView、DashboardView、EventHistoryManager、FeeRouterView、HealthView、LendingEngineView、LiquidationRiskView、LiquidatorView、ModuleHealthView、PositionView、PreviewView、RegistryView、RewardView、RiskView、StatisticsView、UserView、ValuationOracleView、SystemView）、接口（IPositionView、IPositionViewValuation）、ViewAccessLib、GracefulDegradation、以及相关 Mock、测试与 e2e 脚本。

审查时可对照 **Time-Dependency-Refactor-Guide.md** 全文档（尤其是 Step 1A 清单、Step 2C 分组表格与 PR Checklist）确认本 PR 改动与全局规范一致；View 部分重点看缓存有效性、meta、事件字段是否均为 block 口径且无秒级时间门槛。

### 3.5 跨域高影响改动摘要（非纯 View，但与本 PR 强相关）

虽然本 PR 的主叙事是 View，但为了“View 对齐 + 时间口径规范”能真正闭环落地，本分支同时涉及大量跨模块改动。为避免 reviewer 产生“范围惊吓”，这里把**非 View 的高影响改动**按目录做一次显式归类（详细仍以 diff 为准）：

- **接口与 ABI 面**：`src/interfaces/` 大范围修改（View/模块接口、事件 view 等），对下游 types 生成、前端/链下 consumer 的解码与字段约定会产生直接影响。
- **核心逻辑与依赖库**：`src/core/`、`src/libraries/`、`src/Vault/modules/`、`src/Vault/liquidation/` 中存在大量为“block 口径/缓存 meta/事件字段一致性/权限模型”服务的收敛与重构。
- **系统级能力**：`src/monitor/`（降级）、`src/registry/`（注册表/模块键）、`src/Reward/`（奖励）等同步对齐时间口径与架构约束。
- **配套工程化**：`test/`、`scripts/tests/`、`scripts/e2e/` 大规模更新/新增，确保变更可验证；并同步更新文档与运行说明。

如果希望更快把审查范围收敛，建议 reviewer 先从 `docs/Architecture-Guide.md` + `docs/Usage-Guide/Time-Dependency-Refactor-Guide.md` 的 MUST/Checklist 入手，再回看对应目录下的实现是否满足约束。

### 4. View 相关合约与实现（src/Vault/view、模块与库）

- **View 模块**（`src/Vault/view/modules/*.sol`）：  
  AccessControlView、BatchView、CacheOptimizedView、DashboardView、EventHistoryManager、FeeRouterView、HealthView、LendingEngineView、LiquidationRiskView、LiquidatorView、ModuleHealthView、PositionView、PreviewView、RegistryView、RewardView、RiskView、StatisticsView、SystemView、UserView、ValuationOracleView、ViewCache 等，与架构指南、工作指南中的清单与 MUST 对齐（权限、DataPush、meta、批量错误口径等）。
- **新增/调整**：  
  - `src/Vault/view/modules/SystemRiskView.sol`（system-scoped risk，仅全局阈值/系统参数，无 user/users[]）。  
  - `src/Vault/modules/StatisticsPushManager.sol`（Statistics 严格 B+ 单入口编排，见工作指南 §4.4 / 实施清单）。  
- **库与接口**：ViewAccessLib、DataPushLibrary、DataPushTypes、ViewConstants、GracefulDegradation 等与 View 相关的库/常量/接口的修改与一致性收口。

（具体代码级变更以 diff 为准；本段仅概括范围。）

### 5. 测试（test/）

- **View 相关**：  
  - 各 View 模块的单元/集成测试（如 AccessControlView、BatchView、HealthView、PositionView、StatisticsView、SystemView、ViewCache、EventHistoryManager、LiquidatorView、ModuleHealthView、PreviewView、RegistryView、RewardView 等）。  
  - `StatisticsPushManager.usd8.snapshot.test.ts`（Statistics 严格 B+ / snapshot 行为）。  
  - PositionView 缓存有效性、VaultRouter 缓存一致性、VaultBusinessLogic stats 集成等。
- **其他**：  
  - 清算、Reward、LendingEngine、Registry、Vault 等既有测试的更新以配合接口/权限/事件变更。

### 6. 脚本（scripts/tests、scripts/e2e）

- **新增/重要**  
  - **scripts/tests/view-matrix-selfcheck.ts**  
    - 读取 `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`，解析 §4.x 矩阵小节，与 `scripts/e2e/README.md`、`scripts/tests/README.md` 中的脚本引用做一致性检查；  
    - 运行方式：`pnpm -s hardhat run scripts/tests/view-matrix-selfcheck.ts`；期望输出包含 `✅ OK: matrix coverage looks consistent.`，且不会报出 missing/mismatch 类错误。
  - **scripts/e2e/**  
    - 各 View 的 acceptance 脚本（如 e2e-localhost-systemview-routing、positionview、healthview、statisticsview、viewcache、batch-aggregators、eventhistorymanager、liquidatorview、rewardview、scenario-matrix 等）与工作指南 §4.x 测试矩阵对应。  
  - **scripts/tests/**  
    - viewcache-smoke-local、view-schemeu-smoke-local、lendingengine-smoke-local、deploy-gold-silver、ci-realchain-template 等；  
    - **_addressResolver.ts**、**registry-alignment-report**、**registry-rebind-from-config** 等工具/辅助脚本。
- **修改**  
  - 若干 e2e/local 脚本与 README 的说明、路径、环境要求更新，以与 `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` 及新脚本路径一致。

### 7. 删除/迁移的文件（D）

- **文档迁移（旧路径删除）**  
  - `src/Vault/view/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` → 已迁移至 `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`。
- **其他删除**  
  - `src/Vault/ARCH-STRICT-VaultCore-ABI-Interface-Split.md`  
  - `src/libraries/VaultRouterEventLib.sol`、`VaultRouterModuleLib.sol`、`VaultRouterValidationLib.sol`  
  - `src/libraries/ViewInterfaceLibrary.sol`  
  - `src/monitor/GracefulDegradation.sol`（功能已收敛至 `libraries/GracefulDegradation.sol` 等，以实际 diff 为准）

（若 base 分支已无这些文件，则 PR 中仅体现为「view 分支不包含它们」或重构后的替代实现。）

---

## ⚠️ Breaking changes / 集成影响（必须关注）

本 PR **不是纯文档或纯 View 变更**，包含 ABI/行为级别的调整，可能对前端与链下 View consumer 造成破坏性影响。请在 review/集成时显式核对以下事项：

- **时间口径变化（高概率破坏）**
  - **链上门槛/缓存有效性统一使用 `block.number`**（以 `updateBlock`/`maxAgeBlocks`/`...Blocks` 等表达），避免使用秒级时间戳作为有效性门槛。
  - 下游如存在“按秒级 timestamp 推导 TTL/有效性/刷新间隔”的逻辑，需要改为按区块高度计算。
- **接口/事件/返回 meta 约定变化（ABI 级影响）**
  - `src/interfaces/*.sol` 发生大量变更，可能包含 **函数签名、事件字段、返回结构（meta）** 的调整。
  - 下游需要重新生成 types/ABI，并核对解码字段与语义（尤其是 View meta 的 `isValid`、`updateBlock`/`blockNumber` 一致性）。
- **权限与读策略（Scheme U）**
  - View 的读权限/role gating 按 `docs/Architecture-Guide.md` 的 Scheme U 约束收口；如果链下 consumer/前端默认“无需权限即可读”的假设存在，需要同步调整（或预先授权/白名单）。
- **部署与地址/配置变更**
  - 本 PR 修改 `deployments/*.json`、`frontend-config/*`，可能意味着本地/测试网地址或模块绑定发生变化；集成端务必使用同一套地址源（SSOT）并验证 View 路由与 Registry 绑定一致。

建议 reviewer 将本节视为“破坏性变更清单”，逐项确认：**哪些是本 PR 必须的、哪些可以拆分/延后、哪些需要同步发版说明**。

## 📂 View 相关文件变更清单（本 PR 涉及）

以下为**与 View 直接相关的**修改（M）、新增（??）、删除（D）文件，便于审查时逐项核对。

### 修改（M）

**文档**

- `docs/Offchain-View-Consumer-Retry-Audit-Design.md`
- `docs/Test-Guide/statistics-view-testing-guide.md`
- `docs/Test-Guide/vaultview-testing-guide.md`
- `docs/Test-Guide/view-layer-testing-guide.md`

**合约：src/Vault/view**

- `src/Vault/view/README.md`
- `src/Vault/view/ViewConstants.sol`
- `src/Vault/view/modules/AccessControlView.sol`
- `src/Vault/view/modules/BatchView.sol`
- `src/Vault/view/modules/CacheOptimizedView.sol`
- `src/Vault/view/modules/DashboardView.sol`
- `src/Vault/view/modules/EventHistoryManager.sol`
- `src/Vault/view/modules/FeeRouterView.sol`
- `src/Vault/view/modules/HealthView.sol`
- `src/Vault/view/modules/LendingEngineView.sol`
- `src/Vault/view/modules/LiquidationRiskView.sol`
- `src/Vault/view/modules/LiquidatorView.sol`
- `src/Vault/view/modules/ModuleHealthView.sol`
- `src/Vault/view/modules/PositionView.sol`
- `src/Vault/view/modules/PreviewView.sol`
- `src/Vault/view/modules/RegistryView.sol`
- `src/Vault/view/modules/RewardView.sol`
- `src/Vault/view/modules/RiskView.sol`
- `src/Vault/view/modules/StatisticsView.sol`
- `src/Vault/view/modules/SystemView.sol`
- `src/Vault/view/modules/UserView.sol`
- `src/Vault/view/modules/ValuationOracleView.sol`
- `src/Vault/view/modules/ViewCache.sol`

**合约：Mocks / 接口 / 库**

- `src/Mocks/BatchViewMocks.sol`、`HealthViewMocks.sol`、`MockEventsView.sol`、`MockHealthView.sol`、`MockLendingEngineViewAdapter.sol`、`MockLiquidationEventsView.sol`、`MockPositionView.sol`、`MockRewardViewDeps.sol`、`MockRiskViewDeps.sol`、`MockStatisticsView.sol`、`MockStatisticsViewUserSnapshot.sol`、`MockUserViewDeps.sol`、`MockVaultCoreView.sol`、`UserViewSelectorHarness.sol`
- `src/interfaces/ILiquidationEventsView.sol`、`IOrderEngineViewAdapter.sol`、`IPositionView.sol`、`IPositionViewValuation.sol`
- `src/libraries/ViewAccessLib.sol`

**测试**

- `test/StatisticsView.active-users.test.ts`、`StatisticsView.comprehensive.test.ts`、`StatisticsView.guarantee-aggregation.test.ts`、`StatisticsView.migration.test.ts`
- `test/Vault/view/AccessControlView.test.ts`、`BatchView.test.ts`、`CacheOptimizedView.test.ts`、`DashboardView.test.ts`、`FeeRouterView.test.ts`、`HealthView.test.ts`、`LendingEngineView.test.ts`、`LiquidationRiskView.test.ts`、`PositionView.cache-validity.test.ts`、`RiskView.test.ts`、`SystemView.test.ts`、`UserView.test.ts`、`ValuationOracleView.test.ts`、`VaultRouter.cache-consistency.test.ts`、`ViewCache.test.ts`
- `test/Vault/view/modules/EventHistoryManager.test.ts`、`LiquidatorView.test.ts`、`ModuleHealthView.test.ts`、`MultiAssetIntegration.test.ts`、`PreviewView.test.ts`、`RegistryView.test.ts`、`RewardView.test.ts`
- `test/Vault/viewContractAddrVar.comprehensive.test.ts`

**脚本（e2e / tests / utils）**

- `scripts/e2e/e2e-localhost-accesscontrolview-acceptance.ts`、`e2e-localhost-feerouterview-acceptance.ts`、`e2e-localhost-full-with-views.ts`、`e2e-localhost-healthview-acceptance.ts`、`e2e-localhost-lendingengineview-acceptance.ts`、`e2e-localhost-liquidatorview-acceptance.ts`、`e2e-localhost-modulehealthview-acceptance.ts`、`e2e-localhost-positionview-acceptance.ts`、`e2e-localhost-previewview-acceptance.ts`、`e2e-localhost-statisticsview-acceptance.ts`、`e2e-localhost-systemview-routing.ts`、`e2e-localhost-userview-acceptance.ts`、`e2e-localhost-valuationoracleview-acceptance.ts`、`e2e-localhost-viewcache-acceptance.ts`
- `scripts/e2e/utils/systemview-route-assert.ts`、`view-preflight.ts`、`view-scan.ts`

### 新增（??）

- `.pr-description-view.md`（本 PR 描述文件）
- `docs/Units-And-Conversions-SSOT.md`
- `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`（自 `src/Vault/view/` 迁移）
- `docs/Usage-Guide/No-Empty-Blocks-Findings.md`
- `docs/Usage-Guide/StatisticsView-Strict-B-Push-Pipeline-Implementation-Checklist.md`
- `docs/Usage-Guide/Time-Dependency-Refactor-Guide.md`
- `docs/Usage-Guide/User-Dimensional-View-Read-Policy-Guide.md`
- `scripts/e2e/e2e-localhost-rewardview-acceptance.ts`
- `scripts/e2e/utils/registry-alignment-report.ts`
- `scripts/e2e/utils/registry-rebind-from-config.ts`
- `scripts/tests/_addressResolver.ts`
- `scripts/tests/ci-realchain-template.sh`
- `scripts/tests/deploy-gold-silver.ts`
- `scripts/tests/lendingengine-smoke-local.ts`
- `scripts/tests/smoke-productionlike-local.autonode.ts`
- `scripts/tests/view-matrix-selfcheck.ts`
- `scripts/tests/view-schemeu-smoke-local.ts`
- `scripts/tests/viewcache-smoke-local.ts`
- `src/Mocks/MockAccessControlManagerTrapHasRole.sol`
- `src/Mocks/MockAccessControlManagerTrapRequireRole.sol`
- `src/Mocks/MockPositionViewRoleGated.sol`
- `src/Mocks/MockRegistryDynamicModuleKeyRevert.sol`
- `src/Vault/modules/StatisticsPushManager.sol`
- `src/Vault/view/modules/SystemRiskView.sol`
- `test/StatisticsPushManager.usd8.snapshot.test.ts`

### 删除（D）

- `src/Vault/view/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`（已迁移至 `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`）
- `src/libraries/ViewInterfaceLibrary.sol`

---

## ✅ 建议的验证步骤（给 Reviewer）

1. **文档与路径**  
   - 确认 `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` 存在且为唯一工作指南正文。  
   - 全文搜索 `src/Vault/view/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md`，应无剩余引用。

2. **View 矩阵自检**  
   ```bash
   pnpm -s hardhat run scripts/tests/view-matrix-selfcheck.ts
   ```  
   - 期望：脚本输出包含 `✅ OK: matrix coverage looks consistent.`，并且不会报出 missing section / missing script / mismatched counts 等错误。

3. **E2E / 本地链（可选但推荐）**  
   - 按 `scripts/e2e/README.md` 与 `scripts/tests/README.md` 运行本地链与相关 e2e/acceptance 脚本（如 SystemView、PositionView、HealthView、StatisticsView、ViewCache 等），确认与工作指南 §4.x 对应脚本可跑通。

4. **合约与测试**  
   - 运行项目既有 test 套件（如 `pnpm test` 或仓库约定命令），确认无回归。  
   - 重点查看 View 相关与 StatisticsPushManager 的变更及对应测试。

5. **运行/部署/依赖变更核对（强烈建议）**
   - 本 PR 修改了 `package.json`、`hardhat.config.ts`，以及 `deployments/*.json`、`frontend-config/*`。
   - 集成与审查时建议至少执行：
     - `pnpm install`（确保依赖与脚本环境一致）
     - 重新生成/加载最新 ABI（若前端或链下 consumer 依赖接口/事件）
     - 运行一次本地部署脚本（见 `scripts/deploy/` 与 `scripts/deployments/localhost.json`）以确认 Registry/地址源一致

---

## 🔍 审查要点（Checklist）

- [ ] **文档**：ARCH-VIEW-ALIGNMENT-WORKGUIDE 仅存在于 `docs/Usage-Guide/`，且所有引用已更新。
- [ ] **View 行为**：权限（Scheme U）、DataPush、B 类 meta、批量错误口径与 Architecture-Guide / 工作指南一致。
- [ ] **时间依赖（全局规范）**：时间依赖改造为全仓库规范（见 `docs/Usage-Guide/Time-Dependency-Refactor-Guide.md`）；本 PR 涉及其中一部分（含 View 层）。View 及本 PR 改动范围内：缓存 TTL/有效性/同步间隔均为 block 口径（`block.number`、`updateBlock`、`...Blocks`），无秒级时间门槛，与指南中 DONE 清单一致。
- [ ] **Breaking changes/集成影响**：已核对本 PR 的 ABI/事件/时间口径/权限策略变化对前端与链下 consumer 的影响，并在需要处补充迁移说明或同步更新解析逻辑。
- [ ] **测试矩阵**：§4.x 与 `scripts/e2e/*-acceptance*`、`scripts/tests/*` 的对应关系清晰，view-matrix-selfcheck 通过。
- [ ] **删除/迁移**：旧路径文档与废弃库的删除合理，无遗漏引用或误删仍在使用的内容。
- [ ] **Statistics 严格 B+**：StatisticsPushManager 与 StatisticsView 的 snapshot/单入口编排、事件与权限符合实施清单与工作指南。

---

## 📁 关键文件索引（便于跳转）

| 类别 | 路径 |
|------|------|
| View 工作指南（新位置） | `docs/Usage-Guide/ARCH-VIEW-ALIGNMENT-WORKGUIDE.md` |
| 时间依赖改造指南（**全局**，含各段 DONE 清单） | `docs/Usage-Guide/Time-Dependency-Refactor-Guide.md` |
| 矩阵自检脚本 | `scripts/tests/view-matrix-selfcheck.ts` |
| View 模块目录 | `src/Vault/view/modules/` |
| Statistics 实施清单 | `docs/Usage-Guide/StatisticsView-Strict-B-Push-Pipeline-Implementation-Checklist.md` |
| E2E 说明 | `scripts/e2e/README.md` |
| 测试脚本说明 | `scripts/tests/README.md` |
| View 验收报告（依据文档） | `src/Vault/view/README.md` |

---

**说明**：本 PR 描述基于 view 分支当前改动整理，若合并前还有新增提交，请以最新 diff 为准。建议同事审查时优先过一遍「文档迁移与引用」和「view-matrix-selfcheck」，再按模块查看合约与测试变更。
