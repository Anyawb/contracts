import { expect } from 'chai';

// 当前架构未在 VaultRouter 内实现“合约级别用户隔离”。
// 占位测试：保持测试通过，待未来实现隔离后补充真实用例。

describe('Contract-level user isolation (placeholder)', function () {
  it('当前架构未提供合约级用户隔离（占位测试）', async function () {
    expect(true).to.equal(true);
  });
});


